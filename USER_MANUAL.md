# PDF OCR Tool 使用说明书

**版本**: 2.0.0 | **更新日期**: 2026-07-06

---

## 目录

1. [简介](#1-简介)
2. [安装与启动](#2-安装与启动)
3. [界面概览](#3-界面概览)
4. [基本工作流程](#4-基本工作流程)
5. [PDF 文件管理](#5-pdf-文件管理)
6. [框选识别区域](#6-框选识别区域)
7. [字段配置](#7-字段配置)
8. [图像预处理](#8-图像预处理)
9. [模板管理](#9-模板管理)
10. [试识别与批量识别](#10-试识别与批量识别)
11. [识别结果管理](#11-识别结果管理)
12. [OCR 引擎切换](#12-ocr-引擎切换)
13. [导出数据](#13-导出数据)
14. [历史记录](#14-历史记录)
15. [撤销与重做](#15-撤销与重做)
16. [键盘快捷键](#16-键盘快捷键)
17. [配置文件说明](#17-配置文件说明)
18. [常见问题](#18-常见问题)

---

## 1. 简介

PDF OCR Tool 是一款基于人工智能的 PDF 文档文字识别工具。它能够从 PDF 文件中自动提取指定区域的文字内容，广泛应用于发票识别、合同提取、表格数据采集等场景。

### 核心功能

- **双引擎架构**: 支持 PaddleOCR-VL（GPU 智能识别）和 RapidOCR（CPU 轻量识别），可随时切换
- **可视化框选**: 直接在 PDF 页面上拖拽框选需要识别的区域
- **批量处理**: 一次加载多个 PDF，自动分配识别
- **智能匹配**: PaddleOCR-VL 模式下，三级降级策略确保字段精准提取
- **图像预处理**: 支持旋转、亮度、对比度、二值化等预处理
- **模板复用**: 保存区域设置为模板，新文件自动应用
- **历史记录**: 自动保存识别结果，随时回顾和恢复
- **数据导出**: 一键导出为 Excel 格式（含置信度）

### OCR 引擎对比

| 特性 | PaddleOCR-VL（GPU） | RapidOCR（CPU） |
|------|---------------------|-----------------|
| 硬件需求 | NVIDIA GPU（建议 4GB+ 显存） | 无需 GPU |
| 识别能力 | 文字、表格、公式、手写体、印章 | 印刷体文字 |
| 版面理解 | ✅ 自动理解版面结构 | ❌ 逐区域独立识别 |
| 识别方式 | 整页一次推理 | 逐区域裁剪后识别 |
| 速度（单页） | ~2-5 秒 | ~1-3 秒（取决于区域数量） |
| 适用场景 | 复杂文档、发票、混合排版 | 简单格式、固定位置文字 |

---

## 2. 安装与启动

### 快速启动（已安装环境）

```bash
# Windows 双击运行
run.bat

# Git Bash 运行
./run.sh
```

启动器会自动检测 GPU 环境：如 GPU 可用，默认使用 PaddleOCR-VL 引擎；否则自动回退到 CPU 模式。

```bash
# 强制使用 CPU 模式
run.bat --cpu
```

### 首次安装

如果还没有安装 Python 环境，运行一键安装脚本：

```bash
setup_env.bat
```

此脚本会自动创建两个独立环境：
- `venv/` — PaddleOCR-VL GPU 主环境
- `venv-cpu/` — RapidOCR CPU 备用环境

### 软件界面启动

启动后，首先会显示加载画面，OCR 引擎在后台异步初始化。初始化完成后，加载画面自动关闭，进入工作区。

如初始化失败，加载画面会显示具体错误原因和解决步骤。

---

## 3. 界面概览

软件界面采用三栏布局，左侧导航栏可在三个页面间切换：

### 导航栏

| 图标 | 页面 | 用途 |
|------|------|------|
| ✏️ 工作区 | 模板编辑 | 主要工作区：上传PDF、框选区域、试识别 |
| 📊 识别结果 | 结果查看 | 查看和编辑批量识别结果 |
| 📋 历史记录 | 历史回顾 | 浏览和恢复过往识别记录 |

### 工作区三栏布局

```
┌──────────┬───────────────────┬──────────────┐
│  文件列表  │     PDF 预览区      │   字段配置    │
│  (左栏)   │     (中栏)         │   (右栏)     │
│          │                   │              │
│ □ 发票1   │   ┌─────────┐    │ 字段名 类型   │
│ □ 发票2   │   │ PDF页面  │    │ 发票号 文本   │
│ □ 发票3   │   │         │    │ 日期   日期   │
│          │   │  [框1]  │    │ 金额   数字   │
│          │   │  [框2]  │    │              │
│          │   └─────────┘    │              │
└──────────┴───────────────────┴──────────────┘
```

- **文件列表（左栏）**: 显示所有已加载的 PDF 文件，支持拖拽添加
- **PDF 预览区（中栏）**: 显示当前选中的 PDF 页面，可拖拽框选区域
- **字段配置（右栏）**: 管理和配置识别字段

### 顶部工具栏

```
[上传PDF] [试识别] [批量识别] | [保存模板] [加载模板]    [PaddleOCR-VL ▼] ● GPU
```

- 左侧：操作按钮组
- 右侧：引擎切换下拉框 + GPU/CPU 状态指示器

### 底部状态栏

显示当前操作状态和键盘快捷键提示。

---

## 4. 基本工作流程

一个完整的识别流程通常包含以下步骤：

```
第1步: 上传 PDF → 第2步: 框选区域 → 第3步: 试识别验证
                                              ↓
第6步: 导出结果 ← 第5步: 查看/编辑 ← 第4步: 批量识别
```

### 快速上手（5 分钟）

1. **上传 PDF**: 点击工具栏「上传 PDF」或按 `Ctrl+O`
2. **框选区域**: 在 PDF 页面上按住鼠标左键拖拽，画出需要识别的区域框
3. **命名字段**: 在右侧字段配置中为每个框输入字段名（如"发票号码"）
4. **试识别**: 点击「试识别」或按 `Ctrl+T` 验证效果
5. **批量识别**: 确认无误后点击「批量识别」或按 `Ctrl+Enter`
6. **导出**: 切换到「识别结果」页面，点击「导出 Excel」

---

## 5. PDF 文件管理

### 添加文件

- **按钮上传**: 点击工具栏「上传 PDF」，在文件对话框中选择一个或多个 PDF 文件
- **拖拽上传**: 直接将 PDF 文件从资源管理器拖入左侧文件列表面板
- **快捷键**: `Ctrl+O`

### 文件列表

左侧文件列表显示所有已加载的 PDF 文件。每个文件名前有状态颜色标记：

| 颜色 | 含义 |
|------|------|
| 白色/透明 | 未配置识别区域 |
| 浅蓝色 | 有自定义配置（与默认模板不同） |
| 浅绿色 | 使用默认模板配置 |

### 文件操作

- **切换文件**: 点击文件名，中间 PDF 预览区会切换到对应 PDF
- **移除单个**: 选中文件后点击「移除选中」按钮
- **清空全部**: 点击「清空全部」按钮

> 💡 **提示**: 添加的第一个 PDF 文件会作为样本文件，你可以在此基础上框选区域、建立默认模板。

---

## 6. 框选识别区域

### 基本操作

在 PDF 预览区中，你可以通过鼠标操作来框选需要识别的区域：

| 操作 | 方式 | 效果 |
|------|------|------|
| **框选区域** | 按住鼠标左键拖拽 | 在 PDF 上画出一个矩形识别区域 |
| **移动区域** | 拖拽区域中心或边框 | 调整区域位置 |
| **调整大小** | 拖拽区域四角的手柄 | 调整区域宽高 |
| **选中区域** | 单击区域边框 | 高亮选中，同步到字段面板 |
| **删除区域** | 选中后按 `Delete` 键 | 删除该区域 |

### 画布操作

| 操作 | 方式 |
|------|------|
| **平移画布** | 按住鼠标右键拖拽 |
| **缩放画布** | 鼠标滚轮（0.1x ~ 10x） |

### 区域坐标说明

所有框选区域使用**归一化坐标**（0~1 比例），即区域位置相对于 PDF 页面大小的百分比。这意味着：
- 在不同 DPI 渲染下，区域始终对应 PDF 上的同一位置
- 模板可以跨文件复用（即使文件渲染尺寸不同）

---

## 7. 字段配置

字段配置面板位于工作区右侧，用于管理识别字段。

### 字段属性

| 属性 | 说明 | 可选值 |
|------|------|--------|
| **字段名** | 识别字段的名称 | 双击编辑，如"发票号码" |
| **字段类型** | 用于结果校验 | text / number / date / email / phone |
| **识别结果** | 试识别后的文字内容 | 自动填充 |
| **操作** | 删除该字段 | 点击删除按钮 |

### 字段类型说明

选择正确的字段类型可以自动校验识别结果：

| 类型 | 校验规则 | 示例 |
|------|---------|------|
| text | 无校验 | 任意文字 |
| number | 仅数字和小数点 | 8800.00 |
| date | 日期格式（自动标准化为 YYYY-MM-DD） | 2024-01-15 |
| email | 邮箱格式 | example@mail.com |
| phone | 中国大陆手机号（11 位） | 13800138000 |

如果试识别结果不符合类型校验规则，结果会以**红色**标记。

### 置信度标记

| 颜色 | 含义 |
|------|------|
| 🟢 绿色 | 置信度 ≥ 70%，结果可靠 |
| 🟡 黄色 | 置信度 50%-70%，建议核对 |
| 🟠 橙色 | PaddleOCR-VL 关键词兜底匹配 |
| 🔴 红色 | 置信度 < 50%，建议人工录入 |

### 默认模板

当你为第一个 PDF 配置好字段后，可以点击「设为默认模板」按钮。之后加载的新 PDF 会自动应用这套字段配置，无需重复框选。

- **默认模板**: 所有 PDF 共享的字段配置（绿色标记）
- **自定义配置**: 某个 PDF 独有的字段配置（蓝色标记），优先级高于默认模板

---

## 8. 图像预处理

在 PDF 预览区上方有图像预处理工具栏，用于优化识别效果。

### 预处理选项

| 功能 | 参数范围 | 说明 |
|------|---------|------|
| **旋转** | 0°/90°/180°/270° | 旋转 PDF 页面方向 |
| **亮度** | 10% ~ 200% | 调整画面亮度 |
| **对比度** | 10% ~ 200% | 调整画面对比度 |
| **二值化** | 关闭 / 128 / 150 / 180 / 自动 | 将图像转为黑白，适合模糊文档 |
| **自动对比度** | 一键 | 自动优化对比度 |
| **锐化** | 一键 | 增强文字边缘清晰度 |

### 预处理策略

- **每个 PDF 独立设置**: 预处理参数按 PDF 文件独立存储
- **实时预览**: 修改参数后 PDF 预览区立即更新
- **应用到全部**: 点击「应用到全部」按钮，将当前设置同步到所有已加载的 PDF
- **重置**: 点击重置按钮恢复原始状态

> 💡 **提示**: 对于扫描件或拍照件，建议先尝试「自动对比度」+「锐化」组合。对于模糊文档，可尝试「二值化-自动」模式。

---

## 9. 模板管理

模板是字段配置的持久化存储，可以让你的工作成果复用。

### 保存模板

1. 配置好所有字段后，点击工具栏「保存模板」
2. 选择保存位置，文件格式为 `.json`
3. 或使用快捷键 `Ctrl+S`

### 加载模板

1. 点击工具栏「加载模板」
2. 选择一个之前保存的 `.json` 模板文件
3. 会弹出一个**模板预览对话框**，显示模板名称、字段列表等信息
4. 确认无误后点击「加载模板」

### 模板内容

模板 JSON 文件包含以下信息：
- 模板名称和描述
- 所有字段的名称、位置、类型
- 创建时间
- PDF 页面参考尺寸

---

## 10. 试识别与批量识别

### 试识别（单文件）

用于验证当前 PDF 的字段配置是否正确：

1. 确保已加载 PDF 并配置了至少一个识别区域
2. 点击「试识别」或按 `Ctrl+T`
3. OCR 引擎处理当前 PDF，结果显示在字段面板的「识别结果」列中
4. 检查识别结果是否符合预期
5. 如需调整：修改区域位置、预处理参数，再次试识别

> 💡 **提示**: 试识别结果会缓存。切换文件后再切回来，之前的结果仍然保留。

### 批量识别（多文件）

用于一次性处理所有已加载的 PDF：

1. 确保所有 PDF 都有对应的字段配置（默认模板或自定义配置）
2. 点击「批量识别」或按 `Ctrl+Enter`
3. 弹出**进度对话框**，显示处理进度和当前文件名
4. 等待处理完成（可随时点击「取消」中断）

### 中断恢复

如果在批量识别过程中点击取消：

- 弹出**取消处理对话框**，提供以下选项：

| 选项 | 说明 |
|------|------|
| 查看结果 | 切换到结果页面查看已完成的部分 |
| 导出已完成 | 将已完成的结果导出为 Excel |
| 继续识别 | 继续处理剩余文件 |
| 保存并退出 | 保存进度，下次启动时继续 |
| 关闭 | 放弃所有结果 |

如果选择了「保存并退出」，下次启动软件时会自动提示恢复未完成的任务。

---

## 11. 识别结果管理

批量识别完成后，结果自动加载到「识别结果」页面。

### 结果表格

表格列结构：

| 源文件 | 发票号码 | 开票日期 | 金额 | ... | 状态 | 操作 |
|--------|---------|---------|------|-----|------|------|

- **颜色标记**: 每个单元格根据置信度或匹配级别显示不同背景色
- **可编辑**: 双击任意单元格直接修改文字内容
- **已编辑标记**: 手动修改过的单元格显示蓝色背景

### PaddleOCR-VL 匹配级别

使用 PaddleOCR-VL 引擎时，每个字段会显示匹配方式：

| 级别 | 匹配方式 | 颜色 | 可靠性 |
|------|---------|------|--------|
| Level 1 | IoU 精确坐标匹配 | 🟢 绿色 | 最高 |
| Level 2 | 就近搜索匹配 | 🟡 黄色 | 较高 |
| Level 3 | 关键词模糊匹配 | 🟠 橙色 | 一般 |

### 筛选功能

- **文本搜索**: 在搜索框中输入关键字，筛选匹配的行
- **字段筛选**: 下拉选择具体字段，再输入关键字精准筛选
- **低置信度筛选**: 点击「显示低置信度」按钮，仅显示置信度 < 70% 的项

### 重置功能

- **重置单行**: 点击某行的「重置」按钮，恢复该文件的所有字段为 OCR 原始结果
- **重置全部**: 点击「重置所有修改」按钮，恢复所有手动编辑

---

## 12. OCR 引擎切换

工具栏右侧的下拉框可以随时切换 OCR 引擎。

### 切换步骤

1. 点击引擎下拉框，选择目标引擎
2. 在确认对话框中点击「确认切换」
3. 新引擎在后台加载（PaddleOCR-VL 首次加载约需 30-60 秒）
4. 加载完成后 GPU 状态指示器更新

### 状态指示器说明

| 显示 | 含义 |
|------|------|
| 🟢 GPU: PaddleOCR-VL \| VRAM 2.3/8.0 GB | GPU 引擎就绪，显示显存使用 |
| 🔵 GPU: PaddleOCR-VL (就绪) | GPU 引擎就绪（无法获取显存数据） |
| ⚪ CPU: RapidOCR (就绪) | CPU 引擎就绪 |
| 🔴 引擎加载中... | 引擎正在初始化 |
| ⚫ 引擎未初始化 | 引擎尚未加载 |

> ⚠️ **注意**: 切换引擎后，之前的试识别结果会被清空，需要重新识别。

---

## 13. 导出数据

### Excel 导出

1. 切换到「识别结果」页面
2. 点击「导出 Excel」按钮
3. 选择保存位置和文件名
4. 导出文件包含以下列：

```
源文件 | 状态 | 字段1 | 字段1_置信度 | 字段2 | 字段2_置信度 | ...
```

- 每个字段有两列：识别文字 + 置信度百分比
- 可以在 `config.yaml` 中设置 `export.include_confidence: false` 来隐藏置信度列

### 导出时机

- **批量识别完成后**: 自动显示在结果页面，点击导出
- **批量识别中断后**: 取消对话框中有「导出已完成」选项，可导出部分结果

---

## 14. 历史记录

软件自动保存每次批量识别的结果，方便日后回顾。

### 查看历史

1. 点击左侧导航栏「历史记录」
2. 左侧列表显示所有历史记录（最多 10 条）
3. 点击某条记录，右侧显示详情：
   - 识别时间
   - 文件数量和成功/失败统计
   - 识别的字段名称
   - 导出路径（如有）

### 恢复历史

1. 在历史记录列表中选中一条记录
2. 点击「恢复此结果」
3. 识别结果会恢复到「识别结果」页面，可以重新查看和导出

### 管理历史

- **刷新**: 重新加载历史记录列表
- **清空历史**: 删除所有历史记录

---

## 15. 撤销与重做

软件支持对区域操作进行撤销和重做，最多记录 20 步操作。

### 支持的操作

| 操作 | 撤销效果 |
|------|---------|
| 框选新区域 | 删除该区域 |
| 删除区域 | 恢复该区域 |
| 移动/调整区域 | 恢复到移动前位置 |
| 修改字段名 | 恢复到修改前名称 |
| 清空当前字段 | 恢复所有字段 |
| 清空全部字段 | 恢复所有字段 |

### 快捷键

| 快捷键 | 操作 |
|--------|------|
| `Ctrl+Z` | 撤销（Undo） |
| `Ctrl+Y` | 重做（Redo） |

> 💡 **提示**: 执行新的操作后，之前的"重做"历史会被清除。

---

## 16. 键盘快捷键

| 快捷键 | 功能 | 使用场景 |
|--------|------|---------|
| **Ctrl+O** | 上传 PDF | 添加文件 |
| **Ctrl+S** | 保存模板 | 保存当前字段配置 |
| **Ctrl+T** | 试识别 | 验证当前 PDF 识别效果 |
| **Ctrl+Enter** | 批量识别 | 开始处理所有文件 |
| **Delete** | 删除字段 | 选中字段后删除 |
| **Ctrl+Z** | 撤销 | 撤销上一步操作 |
| **Ctrl+Y** | 重做 | 重做已撤销的操作 |

所有快捷键提示显示在底部状态栏。

---

## 17. 配置文件说明

配置文件 `config.yaml` 位于程序根目录，可以在文本编辑器中修改。

### 完整配置项

```yaml
app:
  name: "PDF OCR Tool"       # 窗口标题
  version: "2.0.0"
  window_size: [1600, 1000]  # 默认窗口大小

pdf:
  render_dpi: 200            # PDF 渲染分辨率（越高越清晰，但更占内存）
  max_preview_size: 2000     # 预览图片最大尺寸

ocr:
  engine: "paddleocr_vl"     # 默认引擎: paddleocr_vl / rapidocr
  lang: "ch"                 # 识别语言
  use_gpu: true              # 是否使用 GPU

  # PaddleOCR-VL 引擎配置
  paddleocr_vl:
    vl_rec_model_name: "PaddleOCR-VL-1.6-0.9B"  # 模型名称
    device: "gpu:0"          # GPU 设备
    precision: "fp16"        # 推理精度(fp16节省显存, fp32更精确)
    warmup_on_startup: true  # 启动时预热模型
    idle_unload_seconds: 300 # 空闲卸载时间(秒，0=不卸载)
    page_dpi: 200            # 渲染 DPI
    high_quality_dpi: 300    # 高质量模式 DPI
    match_iou_threshold: 0.5 # IoU 匹配阈值
    match_neighbor_radius: 50 # 就近搜索半径(像素)

  # RapidOCR 引擎配置
  rapidocr:
    use_gpu: false           # RapidOCR GPU 加速
    lang: "ch"               # 识别语言
    det_db_box_thresh: 0.3   # 检测阈值
    drop_score: 0.5          # 置信度过滤

batch:
  max_workers: 4             # 并行处理线程数
  retry_times: 2             # 失败重试次数

export:
  default_format: "xlsx"     # 默认导出格式
  include_confidence: true   # 是否包含置信度列
  include_source_file: true  # 是否包含源文件列
```

### 环境变量

| 变量名 | 作用 | 取值 |
|--------|------|------|
| `PDFOCR_ENGINE` | 覆盖配置文件中的引擎选择 | `paddleocr_vl` / `rapidocr` |

启动器脚本 `run.bat` / `run.sh` 会自动设置此环境变量。

---

## 18. 常见问题

### Q: 启动后停留在加载画面不动？

**A**: OCR 引擎初始化失败。加载画面会显示具体错误和解决步骤：
- 如果是 PaddlePaddle/CUDA 错误，勾选「使用 CPU 模式运行」可切换到 RapidOCR
- 如果是 ONNX/DLL 错误，可点击「一键下载运行库」安装 VC++ 运行库
- 点击「导出诊断信息」可生成诊断文件用于排查问题

### Q: 为什么实际用的是 RapidOCR 而不是 PaddleOCR-VL？

**A**: 程序会自动检测 PaddlePaddle GPU 环境是否可用。如果 GPU 环境不存在，会自动降级到 RapidOCR。要启用 GPU 模式：
1. 运行 `setup_env.bat` 安装 GPU 环境
2. 确保 NVIDIA 驱动正常工作（运行 `nvidia-smi` 检查）

### Q: 识别结果不准确怎么办？

1. **调整区域框**: 确保区域框紧贴目标文字，不要留太多空白
2. **调整预处理**: 尝试调整亮度/对比度，或使用二值化
3. **提高渲染 DPI**: 在 `config.yaml` 中提高 `paddleocr_vl.page_dpi`（如改为 300）
4. **降低匹配阈值**: 减小 `match_iou_threshold` 使坐标匹配更宽松
5. **添加关键词**: 在模板区域配置中添加 `match_keywords` 辅助匹配

### Q: 批量识别到一半中断了怎么办？

**A**: 软件支持任务中断恢复：
- 点击取消后选择「保存并退出」
- 下次启动时自动提示恢复未完成的任务
- 也可以选择「导出已完成」保存部分结果

### Q: 如何让多个 PDF 共享同一套字段配置？

**A**: 使用默认模板功能：
1. 在第一个 PDF 上配置好字段
2. 点击右侧「设为默认模板」按钮
3. 后续加载的 PDF 自动应用此配置

### Q: 模板可以跨电脑使用吗？

**A**: 可以。保存的 `.json` 模板文件可以复制到其他电脑，通过「加载模板」导入。注意模板使用归一化坐标，在不同电脑上无需调整。

### Q: 显存不足怎么办？

1. 在 `config.yaml` 中设置 `precision: "fp16"`（默认已开启），显存减半
2. 减少 `batch.max_workers` 降低并发数
3. 切换到 RapidOCR CPU 模式
4. 或启用空闲卸载：`idle_unload_seconds: 300`（5分钟不操作自动释放显存）

### Q: 如何更新 PaddleOCR-VL 模型？

**A**: PaddleOCR-VL 模型会在首次使用时自动从 HuggingFace 下载。如需更新：
1. 手动删除缓存目录（通常在 `~/.paddlex/` 或 `~/.cache/huggingface/`）
2. 重启程序，会自动下载最新版本
3. 更新 `vl_rec_model_name` 配置项可切换不同模型版本
